<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed application's non-sensitive reference data.
     *
     * Never add default administrator credentials here. Create administrators
     * interactively and grant their role explicitly during deployment.
     */
    public function run(): void
    {
        //
    }
}
